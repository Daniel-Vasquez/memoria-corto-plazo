# Connection string de MongoDB Atlas (o local), usado por src/lib/mongodb.ts.
# Copiar a .env (ignorado por git) y completar con el valor real.
# En Vercel: Project Settings > Environment Variables (Production/Preview/Development).
MONGODB_URI=
